<Led to Korean website>: quizlet, twitch

<ip block>: angellist, cloudflare, doordash, ebay

<captcha>: zipcar

<partially ready>: asana, avodocs, brex, calendly, deel, flickr, mixpanel, postgresql, webflow, wework

 <Other Issues>
 auth0: When headless = false, the browser opens up a page where email, password, and button are present.
 When headless = true, the browser opens up a page where only email and button are present.